<!-- <div class="container-fluid bg-black" *ngIf="placeDetails">
  <div class="row text-center">
    <h3 class="grey mb-0"><strong>{{ placeDetails.name }}</strong></h3>
    <div class="fs-2 text-warning mt-0">
      <ng-container *ngFor="let star of [1, 2, 3, 4, 5]; let i = index">
        <i class="bi" [ngClass]="getStarClasses(i, placeDetails.rating)"></i>
      </ng-container>
    </div>
    <span class="text-white fs-6">
      Based on <strong>{{ placeDetails.user_ratings_total }} reviews</strong>
    </span>
    <div class="mt-2">
      <img
        decoding="async"
        class="img-fluid"
        src="https://cdn.trustindex.io/assets/platform/Google/logo-dark.svg"
        width="100"
        height="25"
        alt="Google"
      />
    </div>
  </div>
  <div class="row mt-3" *ngIf="placeDetails.reviews">
    <div>
      <ngb-carousel [showNavigationArrows]="false" [showNavigationIndicators]="false">
        <ng-template ngbSlide *ngFor="let review of placeDetails.reviews">
          <div class="card col-3">
            <div class="card-body">
              <h5 class="card-title">{{ review.author_name }}</h5>
              <p class="card-text">
                {{ review.text }}
              </p>
              <div>
                <ng-container *ngFor="let star of [1, 2, 3, 4, 5]; let i = index">
                  <i class="bi" [ngClass]="getStarClasses(i, review.rating)"></i>
                </ng-container>
              </div>
            </div>
          </div>
        </ng-template>
      </ngb-carousel>
    </div>
  </div>
</div> -->
